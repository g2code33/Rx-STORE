# RX Store — Frontend Environment
# Copy to .env.local and fill in

# Backend API base URL (no trailing slash). Leave empty for offline mock mode.
# Cloudflare Workers: https://api.rxstore.workers.dev/v1
# Local dev: http://localhost:8787/v1
VITE_API_URL=

# Optional: Supabase / other overrides
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
